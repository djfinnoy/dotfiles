# R prompt
options(prompt="R > ", digits=4, show.signif.starts=FALSE)

# Set default mirror for packages
options(repos=structure(c(CRAN="https://cran.uib.no/")))

# Colors in terminal
library(colorout)
